# Copy to `.env` (already in .gitignore) before running `docker compose up`.
#
# SQL Server requires the sa password to be at least 8 characters and contain
# characters from three of these four sets: uppercase, lowercase, digits, symbols.
SA_PASSWORD=WorkTrack!Dev2026
